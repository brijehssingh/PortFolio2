export default function Footer() {
  return (
    <footer className="footer footer-center p-6 text-base-content">
      <aside>
        <p>© {new Date().getFullYear()} BRIJESH SINGH — Built with React + DaisyUI</p>
      </aside>
    </footer>
  );
}
